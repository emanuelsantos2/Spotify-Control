1.0.0.3

Added Settings to the GUI so that the user can switch keys
GUI now shows current song
Added Settings Save and Delete buttons
Added Volume Up/Down Keys set for Key Up and Down as default
Keys now register themselves in the class constructor
Added Methods to Read and Write to settings.ini file

1.0.0.4
Fixed exception error when killing spotify process
Created thread to automatically change the label to the song title(Spotify Window Title)
Added Minimize to Tray Button to the GUI(Needs Fixing)
Fixed bug that caused the app to show the title of other applications
The GUI will now update it's background with the current song album art